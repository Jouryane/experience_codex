[package]
name = "experience-server"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"

[dependencies]
agent-codex = { path = "../../crates/agent-codex" }
experience-core = { path = "../../crates/experience-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tiny_http = "0.12"
