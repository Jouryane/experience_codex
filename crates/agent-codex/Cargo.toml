[package]
name = "agent-codex"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"

[dependencies]
agent-runtime = { path = "../agent-runtime" }
experience-core = { path = "../experience-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
